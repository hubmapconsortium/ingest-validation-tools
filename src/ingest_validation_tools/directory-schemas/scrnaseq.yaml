#scRNA-Seq description
# Expectations
# 1 or more directories with compressed or uncompressed FastQ files

-
  pattern: '[^/]+/[^/]+\.fastq\.gz'
  description: 'Compressed FastQ files'
  required: True

-
  pattern: '[^/]+/[^/]+\.fastq'
  description: 'FastQ files'
  required: False
