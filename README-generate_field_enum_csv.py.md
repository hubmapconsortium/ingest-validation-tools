```
usage: generate_field_enum_csv.py [-h] [--fields FIELD [FIELD ...]]

optional arguments:
  -h, --help            show this help message and exit
  --fields FIELD [FIELD ...]
                        Fields to include in report. Default: ['filename',
                        'field', 'description', 'enum']
```
