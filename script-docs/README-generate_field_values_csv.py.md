```text
usage: generate_field_values_csv.py [-h] [--url URL] [--size SIZE]
                                    [--type TYPE]

optional arguments:
  -h, --help   show this help message and exit
  --url URL    ES endpoint. Default:
               https://search.api.hubmapconsortium.org/portal/search
  --size SIZE  Number of records to pull. Default: 20
  --type TYPE  Entity type to query. Default: Dataset
```
