```text
usage: generate_field_yaml.py [-h] --attr {description,type}

Outputs a YAML dict listing fields and their definitions, or their types.

optional arguments:
  -h, --help            show this help message and exit
  --attr {description,type}
                        Attribute to pull from schemas
```
