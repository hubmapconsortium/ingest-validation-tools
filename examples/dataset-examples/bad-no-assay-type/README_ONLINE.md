```
Preflight: 'No assay data retrieved for examples/dataset-examples/bad-no-assay-type/upload/bad-metadata.tsv.
  Does not contain "assay_type" or "dataset_type". Column headers in TSV: this_does_not_have_data_type,
  assay.'
Hint: 'If validation fails because of extra whitespace in the TSV, try:

  src/cleanup_whitespace.py --tsv_in original.tsv --tsv_out clean.tsv.'
```
