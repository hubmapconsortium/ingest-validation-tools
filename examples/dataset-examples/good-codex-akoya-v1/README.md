```
Metadata TSV Errors:
  examples/dataset-examples/good-codex-akoya-v1/upload/name-just-needs-to-end-with-metadata.tsv (as codex):
    External:
      row 2, data examples/dataset-examples/good-codex-akoya-v1/upload/dataset-1:
        Required but missing:
        - dataset.json
Hint: 'If validation fails because of extra whitespace in the TSV, try:

  src/cleanup_whitespace.py --tsv_in original.tsv --tsv_out clean.tsv'
```
