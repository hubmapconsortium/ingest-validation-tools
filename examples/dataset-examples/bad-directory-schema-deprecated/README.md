```
Metadata TSV Errors:
  examples/dataset-examples/bad-directory-schema-deprecated/upload/name-just-needs-to-end-with-metadata.tsv (as codex):
    External:
      row 2, data examples/dataset-examples/bad-directory-schema-deprecated/upload/dataset-1:
        Deprecated directory schema: codex 0
Hint: 'If validation fails because of extra whitespace in the TSV, try:

  src/cleanup_whitespace.py --tsv_in original.tsv --tsv_out clean.tsv'
```
