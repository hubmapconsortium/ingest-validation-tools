```
Metadata TSV Errors:
  examples/dataset-examples/bad-directory-schema-version/upload/metadata.tsv (as scatacseq):
    External:
      row 2, data examples/dataset-examples/bad-directory-schema-version/upload/dataset-1:
        Undefined directory schema: scatacseq this-version-is-bad
Hint: 'If validation fails because of extra whitespace in the TSV, try:

  src/cleanup_whitespace.py --tsv_in original.tsv --tsv_out clean.tsv'
```
